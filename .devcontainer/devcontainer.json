{
  "name": "llvmlite-demo",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".devcontainer"
  },
  "remoteUser": "vscode",
  "postCreateCommand": "bash -lc 'pip install -U pip && (export LLVM_CONFIG=/usr/bin/llvm-config-15 && pip install -e .) || pip install llvmlite && pip install numpy matplotlib jupyter jupyterlab pytest && python demo/run.py --generate-only'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter",
        "ms-azuretools.vscode-docker"
      ]
    }
  }
}
