{
  "name": "llvmlite-demo",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".devcontainer"
  },
  "remoteUser": "vscode",
  "postCreateCommand": "bash -lc 'export LLVM_CONFIG=/usr/bin/llvm-config-15 && pip install -U pip && pip install -e . && pip install numpy matplotlib jupyter jupyterlab pytest && python demo/run.py --generate-only'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter",
        "ms-azuretools.vscode-docker"
      ]
    }
  }
}
